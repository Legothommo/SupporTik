<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:tb="http://www.hardcodet.net/taskbar"
                    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                    x:Class="SupporTik.Styles.ContextMenu">

    <Style x:Key="TraySeparatorStyle" TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="Margin" Value="8,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Rectangle Height="1" Fill="{TemplateBinding Background}" SnapsToDevicePixels="True"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TrayMenuItemStyle" TargetType="MenuItem">
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="templateRoot"
                            Background="Transparent"
                            CornerRadius="6"
                            SnapsToDevicePixels="True"
                            Margin="2,1">
                        <StackPanel Orientation="Horizontal" Margin="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="IconPresenter"
                                              ContentSource="Icon"
                                              Margin="0,0,10,0"
                                              VerticalAlignment="Center"/>
                            <ContentPresenter ContentSource="Header"
                                              RecognizesAccessKey="True"
                                              VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="templateRoot" Property="Background" Value="{StaticResource SubtleFillBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TrayContextMenuStyle" TargetType="ContextMenu">
        <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorDefaultBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Width" Value="180"/>
        <Setter Property="Padding" Value="6"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource TrayMenuItemStyle}"/>
    </Style>

    <ControlTemplate x:Key="MyContextMenuTemplate" TargetType="ContextMenu">
        <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{StaticResource BorderSubtleBrush}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="4">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Color="Black" Opacity="0.35" ShadowDepth="4"/>
            </Border.Effect>
            <StackPanel IsItemsHost="True"/>
        </Border>
    </ControlTemplate>

    <!-- Трей Контекстное Меню -->
    <ContextMenu x:Key="TrayContextMenu" Template="{StaticResource MyContextMenuTemplate}" Style="{StaticResource TrayContextMenuStyle}">
        <MenuItem Header="Открыть" Command="{Binding ShowMainWindowCommand}">
            <MenuItem.Icon>
                <ui:SymbolIcon Symbol="Apps24"/>
            </MenuItem.Icon>
        </MenuItem>
        <MenuItem Header="Настройки" Command="{Binding OpenSettingsCommand}">
            <MenuItem.Icon>
                <ui:SymbolIcon Symbol="Settings24"/>
            </MenuItem.Icon>
        </MenuItem>
        <MenuItem Header="Включено" Command="{Binding ToggleEnabledCommand}" Tag="EnableToggle">
            <MenuItem.Icon>
                <ui:SymbolIcon Symbol="Power24"/>
            </MenuItem.Icon>
            <MenuItem.Style>
                <Style TargetType="MenuItem" BasedOn="{StaticResource TrayMenuItemStyle}">
                    <Setter Property="Foreground" Value="{StaticResource StatusActiveBrush}"/>
                </Style>
            </MenuItem.Style>
        </MenuItem>
        <Separator Style="{StaticResource TraySeparatorStyle}"/>
        <MenuItem Header="Выход" Click="ExitApplication">
            <MenuItem.Icon>
                <ui:SymbolIcon Symbol="SignOut24"/>
            </MenuItem.Icon>
            <MenuItem.Style>
                <Style TargetType="MenuItem" BasedOn="{StaticResource TrayMenuItemStyle}">
                    <Setter Property="Foreground" Value="{StaticResource StatusPausedBrush}"/>
                </Style>
            </MenuItem.Style>
        </MenuItem>
    </ContextMenu>

    <!-- Иконка Трэя -->
    <tb:TaskbarIcon x:Key="MyNotifyIcon"
                    IconSource="/Assets/app_icon.ico"
                    ToolTipText="SupporTik"
                    ContextMenu="{StaticResource TrayContextMenu}">
    </tb:TaskbarIcon>

</ResourceDictionary>
