<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:tb="http://www.hardcodet.net/taskbar"
                    x:Class="SupporTik.Styles.ContextMenu">

    <Style x:Key="TraySeparatorStyle" TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource BorderColor}"/>
        <Setter Property="Margin" Value="0,2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Rectangle Height="1" Fill="{TemplateBinding Background}" SnapsToDevicePixels="True"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TrayMenuItemStyle" TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="templateRoot" 
                            Background="Transparent"
                            BorderBrush="Transparent"
                            BorderThickness="1"
                            CornerRadius="3"
                            SnapsToDevicePixels="True"
                            Margin="3">
                        <Grid VerticalAlignment="Center">
                            <ContentPresenter x:Name="ContentPresenter"
                                              ContentSource="Header"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                              VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="templateRoot" Property="Background" Value="{StaticResource AccentGreen}"/>
                            <Setter Property="Foreground" Value="{StaticResource BgSurface}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource BorderColor}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TrayContextMenuStyle" TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource BgSurface}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Width" Value="150"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource TrayMenuItemStyle}"/>
    </Style>

    <ControlTemplate x:Key="MyContextMenuTemplate" TargetType="ContextMenu">
        <Border Background="{StaticResource BgSurface}"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1"
                CornerRadius="3">
            <StackPanel IsItemsHost="True"/>
        </Border>
    </ControlTemplate>

    <!-- Трей Контекстное Меню -->
    <ContextMenu x:Key="TrayContextMenu" Template="{StaticResource MyContextMenuTemplate}" Style="{StaticResource TrayContextMenuStyle}">
        <MenuItem Header="Открыть" Click="ShowMainWindow"/>
        <MenuItem Header="Настройки" Click="OpenSettings"/>
        <MenuItem Header="Включено" Click="EnableText">
            <MenuItem.Style>
                <Style TargetType="MenuItem" BasedOn="{StaticResource TrayMenuItemStyle}">
                    <Setter Property="Foreground" Value="{StaticResource AccentGreen}"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource BgSurface}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </MenuItem.Style>
        </MenuItem>
        <Separator Style="{StaticResource TraySeparatorStyle}"/>
        <MenuItem Header="Выход" Click="ExitApplication">
            <MenuItem.Style>
                <Style TargetType="MenuItem" BasedOn="{StaticResource TrayMenuItemStyle}">
                    <Setter Property="Foreground" Value="{StaticResource AccentCoral}"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource BgSurface}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </MenuItem.Style>
        </MenuItem>
    </ContextMenu>

    <!-- Иконка Трэя -->
    <tb:TaskbarIcon x:Key="MyNotifyIcon"
                    IconSource="/Assets/app_icon.ico"
                    ToolTipText="SupporTik"
                    ContextMenu="{StaticResource TrayContextMenu}">
    </tb:TaskbarIcon>

</ResourceDictionary>