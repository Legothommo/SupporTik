<Page x:Class="SupporTik.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:SupporTik.Pages"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="450"
      d:DesignWidth="800"
      Title="SettingsPage">

    <Page.Resources>
        <!-- Стиль подзаголовков секций -->
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <!-- Стиль описания настроек -->
        <Style x:Key="SettingDescriptionStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </Page.Resources>

    <Grid Background="{DynamicResource ApplicationBackgroundBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Hidden"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="24">

                <!-- Заголовок страницы -->
                <TextBlock Text="Настройки"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                           FontSize="22"
                           FontWeight="Bold"
                           Margin="0,0,0,20"/>

                <!-- 1. Секция: Поведение и Запуск -->
                <ui:Card Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="ПОВЕДЕНИЕ И ЗАПУСК"
                                   Style="{StaticResource SectionHeaderStyle}"/>

                        <!-- Автозапуск -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Запускать вместе с Windows"
                                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                           FontSize="14"/>
                                <TextBlock Text="Автоматический запуск программы при входе в систему"
                                           Style="{StaticResource SettingDescriptionStyle}"/>
                            </StackPanel>
                            <ui:ToggleSwitch x:Name="ChkAutoStart"
                                             Grid.Column="1"
                                             VerticalAlignment="Center"
                                             IsChecked="{Binding AutoStartEnabled, Mode=TwoWay}"/>
                        </Grid>

                        <Separator Background="{StaticResource BorderSubtleBrush}"
                                   Margin="0,4,0,12"/>

                        <!-- Сворачивание в трей при закрытии -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Сворачивать в трей при закрытии"
                                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                           FontSize="14"/>
                                <TextBlock Text="При нажатии на крестик окно будет скрываться в системный трей"
                                           Style="{StaticResource SettingDescriptionStyle}"/>
                            </StackPanel>
                            <ui:ToggleSwitch x:Name="ChkMinimizeToTray"
                                             Grid.Column="1"
                                             VerticalAlignment="Center"
                                             IsChecked="{Binding MinimizeToTray, Mode=TwoWay}"/>
                        </Grid>

                        <Separator Background="{StaticResource BorderSubtleBrush}"
                                   Margin="0,4,0,12"/>

                        <!-- Запуск свернутым -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Запускать в свернутом виде"
                                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                           FontSize="14"/>
                                <TextBlock Text="При старте приложения сразу скрывать его в трей"
                                           Style="{StaticResource SettingDescriptionStyle}"/>
                            </StackPanel>
                            <ui:ToggleSwitch x:Name="ChkStartMinimized"
                                             Grid.Column="1"
                                             VerticalAlignment="Center"
                                             IsChecked="{Binding StartMinimized, Mode=TwoWay}"/>
                        </Grid>
                    </StackPanel>
                </ui:Card>

                <!-- 2. Секция: NDA Замена -->
                <ui:Card Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="NDA ЗАМЕНА"
                                   Style="{StaticResource SectionHeaderStyle}"/>
                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="Сочетание клавиш:"
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                       FontSize="12"
                                       Margin="0,0,0,4"/>
                            <Border x:Name="HotkeyCaptureArea"
                                    Background="{DynamicResource ApplicationBackgroundBrush}"
                                    BorderBrush="{StaticResource BorderSubtleBrush}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="8"
                                    Focusable="True"
                                    MouseDown="HotkeyCaptureArea_MouseDown"
                                    LostFocus="HotkeyCaptureArea_LostFocus">
                                <Grid>
                                    <TextBlock x:Name="TbHotkeyDisplay"
                                               Text="{Binding NdaHotkeyDisplay}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                               FontWeight="SemiBold"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </ui:Card>

                <!-- 3. Секция: Меню рекламы -->
                <ui:Card Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="МЕНЮ РЕКЛАМЫ"
                                   Style="{StaticResource SectionHeaderStyle}"/>
                        <TextBlock Text="Открывает окно парсинга рекламных кампаний Яндекс.Бизнес"
                                   Style="{StaticResource SettingDescriptionStyle}"
                                   Margin="0,0,0,8"/>
                        <StackPanel>
                            <TextBlock Text="Сочетание клавиш:"
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                       FontSize="12"
                                       Margin="0,0,0,4"/>
                            <Border x:Name="MarketingHotkeyCaptureArea"
                                    Background="{DynamicResource ApplicationBackgroundBrush}"
                                    BorderBrush="{StaticResource BorderSubtleBrush}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="8"
                                    Focusable="True"
                                    MouseDown="MarketingHotkeyCaptureArea_MouseDown"
                                    LostFocus="MarketingHotkeyCaptureArea_LostFocus">
                                <Grid>
                                    <TextBlock x:Name="TbMarketingHotkeyDisplay"
                                               Text="{Binding MarketingHotkeyDisplay}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                               FontWeight="SemiBold"/>
                                </Grid>
                            </Border>
                        </StackPanel>

                        <Separator Background="{StaticResource BorderSubtleBrush}"
                                   Margin="0,12,0,12"/>

                        <!-- Сторона появления окна -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Открывать слева"
                                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                           FontSize="14"/>
                                <TextBlock Text="Окно меню рекламы выезжает у левого края экрана вместо правого"
                                           Style="{StaticResource SettingDescriptionStyle}"/>
                            </StackPanel>
                            <ui:ToggleSwitch x:Name="ChkMarketingMenuFromLeft"
                                             Grid.Column="1"
                                             VerticalAlignment="Center"
                                             IsChecked="{Binding MarketingMenuFromLeft, Mode=TwoWay}"/>
                        </Grid>
                    </StackPanel>
                </ui:Card>

                <!-- 4. Секция: Обновления -->
                <ui:Card Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="ОБНОВЛЕНИЯ"
                                   Style="{StaticResource SectionHeaderStyle}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="Проверка обновлений"
                                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                           FontSize="14"/>
                                <TextBlock Text="Автоматически проверяется при запуске, но можно и вручную"
                                           Style="{StaticResource SettingDescriptionStyle}"/>
                            </StackPanel>
                            <ui:Button Grid.Column="1"
                                       Content="{Binding CheckUpdatesLabel}"
                                       Icon="{ui:SymbolIcon ArrowSync24}"
                                       Appearance="Secondary"
                                       VerticalAlignment="Center"
                                       Command="{Binding CheckUpdatesCommand}"/>
                        </Grid>
                    </StackPanel>
                </ui:Card>

                <!-- 5. Секция: Импорт и Экспорт -->
                <ui:Card Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="ИМПОРТ И ЭКСПОРТ"
                                   Style="{StaticResource SectionHeaderStyle}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ui:Button Grid.Column="0"
                                       Content="Импорт"
                                       Icon="{ui:SymbolIcon ArrowImport24}"
                                       Appearance="Secondary"
                                       HorizontalAlignment="Stretch"
                                       Margin="0,10,5,10"
                                       Height="34"
                                       Command="{Binding ImportCommand}"/>
                            <ui:Button Grid.Column="1"
                                       Content="Экспорт"
                                       Icon="{ui:SymbolIcon ArrowExport24}"
                                       Appearance="Secondary"
                                       HorizontalAlignment="Stretch"
                                       Margin="5,10,0,10"
                                       Height="34"
                                       Command="{Binding ExportCommand}"/>
                        </Grid>
                    </StackPanel>
                </ui:Card>

                <!-- 6. Секция: Сброс авторизации -->
                <ui:Card Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="АВТОРИЗАЦИЯ"
                                   Style="{StaticResource SectionHeaderStyle}"/>

                        <TextBlock Text="Удалить сохранённую сессию. При следующем открытии меню рекламы потребуется войти заново."
                                   Style="{StaticResource SettingDescriptionStyle}"
                                   Margin="0,0,0,8"/>

                        <ui:Button Content="Выйти"
                                   Icon="{ui:SymbolIcon SignOut24}"
                                   Appearance="Danger"
                                   HorizontalAlignment="Stretch"
                                   Height="34"
                                   Command="{Binding ClearAuthorizationCommand}"/>
                    </StackPanel>
                </ui:Card>

                <!-- 7. Секция: Сброс -->
                <ui:Card Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="СБРОС"
                                   Style="{StaticResource SectionHeaderStyle}"/>
                        <TextBlock Text="Вернуть все настройки к значениям по умолчанию. Бинды не затрагиваются."
                                   Style="{StaticResource SettingDescriptionStyle}"
                                   Margin="0,0,0,8"/>
                        <ui:Button Content="Сбросить настройки"
                                   Icon="{ui:SymbolIcon ArrowReset24}"
                                   Appearance="Danger"
                                   HorizontalAlignment="Stretch"
                                   Height="34"
                                   Command="{Binding ResetCommand}"/>
                    </StackPanel>
                </ui:Card>

                <!-- Кнопка сохранения -->
                <ui:Button x:Name="BtnSave"
                           Content="Сохранить настройки"
                           Icon="{ui:SymbolIcon Save24}"
                           Appearance="Primary"
                           HorizontalAlignment="Right"
                           Command="{Binding SaveCommand}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>